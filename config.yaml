model:
  pipeline_path: "models/pixart_sigma"

generate:
  seed:                   # null = random each run
  output_dir: "outputs"
  min_res: 512
  max_res: 1024
  min_steps: 6
  max_steps: 20
  jitter_low: 0.02
  jitter_mid: 0.05
  jitter_high: 0.10

benchmark:
  ref_time: 0.08
  repeats: 3
  steps: 100
